<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zwy.accounting.dao.AccountEntityMapper">
  <resultMap id="BaseResultMap" type="com.zwy.accounting.entity.AccountEntity">
    <id column="account_id" jdbcType="CHAR" property="accountId" />
    <result column="account_type" jdbcType="INTEGER" property="accountType" />
    <result column="name" jdbcType="VARCHAR" property="name" />
    <result column="amount" jdbcType="DOUBLE" property="amount" />
    <result column="description" jdbcType="VARCHAR" property="description" />
    <result column="creator" jdbcType="CHAR" property="creator" />
  </resultMap>

  <sql id="TABLE">
        tb_account
  </sql>

  <sql id="COLUMN">
    account_id, account_type, `name`, amount, description, creator, gmt_create, gmt_update,
    dr
  </sql>

  <!-- 新增账户 -->
  <insert id="insert" >
    insert into tb_account (account_id, account_type, `name`, amount, description, creator)
    values
    <foreach collection="list" item="item" index="index" separator=",">
      (#{item.accountId}, #{item.accountType}, #{item.name},#{item.amount}, #{item.description}, #{item.creator})
    </foreach>
  </insert>

  <!-- 查询账户 -->
  <select id="selectByCondition"  resultMap="BaseResultMap">
    select 
    <include refid="COLUMN" />
    from
    <include refid="TABLE" />
    where 1=1
    and creator = #{creator}
    and dr = 0

    <if test="accountId != null and accountId != ''" >
      AND account_id = #{accountId}
    </if>

    <if test="accountType != null and accountType != ''" >
      AND account_type = #{accountType}
    </if>

    <if test="name != null and name != ''" >
      AND name = #{name}
    </if>

  </select>

  <!-- 删除用户的某个账户 删除时更新dr=1 不进行物理删除 -->
  <update id="deleteByAccountId" >
    update
    <include refid="TABLE"></include>
    set dr = 1
    where account_id = #{accountId} and creator = #{creator} and dr = 0
  </update>

  <update id="updateByAccountId" parameterType="com.zwy.accounting.entity.AccountEntity">
     update
    <include refid="TABLE"></include>
    <set>
      <if test="accountType != null">
        account_type = #{accountType,jdbcType=INTEGER},
      </if>
      <if test="name != null">
        `name` = #{name,jdbcType=VARCHAR},
      </if>
      <if test="amount != null">
        amount = #{amount,jdbcType=DOUBLE},
      </if>
      <if test="description != null">
        description = #{description,jdbcType=VARCHAR},
      </if>
    </set>
    where account_id = #{accountId,jdbcType=CHAR}
  </update>

  <select id="isExist" parameterType="java.lang.String" resultType="java.lang.String">
    select name from <include refid="TABLE" /> where name = #{name} and dr = 0 limit 1
  </select>
</mapper>