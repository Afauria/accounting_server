<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zwy.accounting.dao.CategoryEntityMapper">
  <resultMap id="BaseResultMap" type="com.zwy.accounting.entity.CategoryEntity">
    <id column="category_id" jdbcType="CHAR" property="categoryId" />
    <result column="category_type" jdbcType="INTEGER" property="categoryType" />
    <result column="name" jdbcType="VARCHAR" property="name" />
    <result column="order1" jdbcType="INTEGER" property="order1" />
    <result column="description" jdbcType="VARCHAR" property="description" />
    <result column="creator" jdbcType="CHAR" property="creator" />
  </resultMap>

  <sql id="TABLE">
    tb_category
  </sql>

  <sql id="COLUMN">
    category_id, category_type, `name`, order1, description, creator, gmt_create, gmt_update, 
    dr
  </sql>

  <insert id="insert" parameterType="com.zwy.accounting.entity.CategoryEntity">
    insert into tb_category (category_id, category_type, `name`, order1, description, creator)
    values
    <foreach collection="list" item="item" index="index" separator=",">
      (#{item.categoryId}, #{item.categoryType}, #{item.name}, #{item.order1}, #{item.description}, #{item.creator})
    </foreach>
  </insert>

  <select id="selectByCondition" parameterType="java.lang.String" resultMap="BaseResultMap">
    select 
    <include refid="COLUMN" />
    from
    <include refid="TABLE" />
    where 1=1
    and creator = #{creator}
    and dr = 0
    <if test="categoryId != null and categoryId != ''">
      AND category_id = #{categoryId}
    </if>

    <if test="categoryType != null">
      AND category_type = #{categoryType}
    </if>

    <if test="name != null">
      AND name = #{name}
    </if>
  </select>

  <update id="updateByCategoryId" parameterType="com.zwy.accounting.entity.CategoryEntity">
    update
    <include refid="TABLE"></include>
    <set>
      <if test="categoryType != null">
        category_type = #{categoryType,jdbcType=INTEGER},
      </if>
      <if test="name != null">
        `name` = #{name,jdbcType=VARCHAR},
      </if>
      <if test="order1 != null">
        order1 = #{order1,jdbcType=INTEGER},
      </if>
      <if test="description != null">
        description = #{description,jdbcType=VARCHAR},
      </if>

    </set>
    where category_id = #{categoryId,jdbcType=CHAR}
  </update>

  <delete id="deleteByCategoryId" parameterType="java.lang.String">
    update
    <include refid="TABLE"></include>
    set dr = 1
    where category_id = #{categoryId,jdbcType=CHAR} and creator = #{creator} and dr = 0
  </delete>


  <select id="isExist" parameterType="java.lang.String" resultType="java.lang.String">
    select name from <include refid="TABLE" /> where name = #{name} and dr = 0 limit 1
  </select>


</mapper>